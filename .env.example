# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/crypto_intel

# GitHub API (optional - increases rate limits)
GITHUB_TOKEN=ghp_your_token_here

# OpenAI API for insights generation
OPENAI_API_KEY=sk-your-key-here
# OPENAI_API_BASE=https://api.openai.com/v1  # Optional: for custom endpoints

# Update Schedule Configuration (for continuous mode)
# Time in minutes between updates for each data source
GITHUB_UPDATE_INTERVAL_MINUTES=360    # 6 hours (GitHub has rate limits)
TWITTER_UPDATE_INTERVAL_MINUTES=30    # 30 minutes (when implemented)
ONCHAIN_UPDATE_INTERVAL_MINUTES=15    # 15 minutes (when implemented)
CHECK_INTERVAL_SECONDS=60             # How often to check if updates are needed (1 minute)
